<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!<br>
Our articles have <%= Comment.public_count %> comments in total.
<br><br>
<div class="article-container">
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <div class="article">
        <%= link_to article.title, article, class: "title" %>
        <br><br>
        <%= article.body.split.first(7).join(' ').concat(' ...') %>
        <br><br>
        <div class="postinfo">
          Posted
          <%= article.created_at %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
<br>
<%= link_to 'New Article', new_article_path %>
